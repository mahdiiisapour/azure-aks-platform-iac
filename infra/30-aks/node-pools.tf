# The user node pool is intentionally deferred.
# The Azure Free Trial quota currently supports only the temporary one-node
# system pool. The existing user subnet remains reserved for a later phase.

